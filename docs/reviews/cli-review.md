### Spec Compliance

- ❌ Issues found: the provider integration violates the required observational calibration timing. `src/btc5m/cli.py:258` records each published snapshot, then `src/btc5m/engine.py:126` changes the same cached snapshot's `now_ms` and `src/btc5m/engine.py:127` records it again. A snapshot received before the calibration window can therefore become its supposedly first received sample. Important finding I1 below includes a real-core reproduction.
- ✅ The requested command surface, strict arguments, explicit execution consent, literal credential parsing, native wrapper, legacy removals, operational documentation and CI are present: `src/btc5m/cli.py:65`, `src/btc5m/cli.py:124`, `src/btc5m/__main__.py:3`, `scripts/btc5m_ctl.sh:3`, `README.md:14`, `SKILL.md:8`, `CONTOUR.md:3`, `examples/run-example.md:1`, `.github/workflows/check.yml:1` and `AGENTS.md:19`. The nine obsolete YAML/compose/script files in the package are complete deletions, with no replacement trading engine or Docker workflow.
- ✅ Binding integration changes are present: immutable `SnapshotInput` (`src/btc5m/execution_types.py:214`), retained invalidation generation (`src/btc5m/cli.py:208`), post-reconciliation provider consumption (`src/btc5m/engine.py:84`), independently refreshed held books (`src/btc5m/engine.py:104`), current BUY authorization (`src/btc5m/engine.py:267`), and persisted safe metadata comparisons (`src/btc5m/market_data.py:596`, `src/btc5m/ledger.py:839`).
- ⚠️ Cannot verify from this diff: completion/results of the root's running 35-minute CLI observation; the final independent whole-branch review and its fixes; final global CHEATSHEET/project-memory completion; the scientific/source-register contents linked by `README.md:66`; exhaustive unchanged SDK/accounting/settlement safety contracts and every unchanged configuration consumer. Root must resolve those stages separately. No funded account/venue/claim verification is implied by the synthetic tests or this review.

### Strengths

- ✅ Authentication and ownership boundaries are explicit. `run --execute` is required by parsing and checked again (`src/btc5m/cli.py:160`, `src/btc5m/cli.py:487`); credential values are absent from representations and exception output (`src/btc5m/cli.py:51`, `src/btc5m/cli.py:102`). Account diagnosis uses a readonly existing journal or isolated temporary state (`src/btc5m/cli.py:442`), while mutating reconciliation acquires the normal owner lock. The real behaviors are exercised at `tests/test_cli.py:175` and `tests/test_cli.py:190`.
- ✅ Startup clears an old stop once before awaited account construction; the independent controller preserves a newer stop and cancels bounded execution work (`src/btc5m/cli.py:494`, `src/btc5m/cli.py:511`, `src/btc5m/cli.py:598`). Unknown order/reserve survival and owner release have a retained behavioral test (`tests/test_cli.py:384`).
- ✅ Exit scheduling addresses the actual latency problem without bypassing reconciliation. The Engine chooses the reconciled position, awaits its book, then refreshes time before expiry routing (`src/btc5m/engine.py:94`). Tests cover six-second account reads against a five-second freshness limit, stale provider data, expiry during the await, and shutdown (`tests/test_engine.py:771`); the actual CLI test blocks discovery while a protected SELL proceeds (`tests/test_cli.py:515`).
- ✅ Ordinary eligible updates do not invalidate every preparation, while generation changes and current principal/reserve/price constraints are checked before POST (`src/btc5m/cli.py:282`, `src/btc5m/engine.py:267`, `tests/test_engine.py:870`). Raw publication continues while account I/O is blocked (`tests/test_cli.py:611`), subject to I1's duplicate consumer recording.
- ✅ End-to-end checks exercise the real core, SDK signing, receipt parsing, and SQLite with synthetic venue transport. They verify actual partial-exit fees, final cash/PnL and unknown restart behavior (`tests/test_end_to_end.py:38`, `tests/test_end_to_end.py:127`). Reports use public Ledger accessors (`src/btc5m/cli.py:329`), and rejected metadata comparisons survive the real producer-to-ledger path (`tests/test_market_data.py:966`).

### Issues

#### Critical (Must Fix)

- ✅ None found in the task-scoped change.

#### Important (Should Fix)

- **I1 / P2 — Preserve publication time and record each raw snapshot once.** `src/btc5m/engine.py:126-127`, introduced into the CLI provider path by `src/btc5m/engine.py:84-85`, duplicates `src/btc5m/cli.py:258`. Engine consumption replaces the cached snapshot timestamp with the later execution time and calls `Ledger.record_snapshot` again. That method uses `snapshot.now_ms` to accept a calibration sample (`src/btc5m/ledger.py:970`) and to record paired decisions (`src/btc5m/ledger.py:944`). Reproduction: publish one eligible snapshot at target minus 500 ms, advance only the synthetic clock to target plus 500 ms, and call the real Engine once without publishing another snapshot. Calibration changes from `pending` to `observed` at target plus 500 ms, and one publication produces four raw decisions instead of two. This pollutes the promised first-received 0–2-second sample and makes raw screen counts depend on account/Engine cadence. With provider mode, keep observational writes at publication and reevaluate execution eligibility at real current time without re-recording that cached input as a newly received observation; preserve the supported direct-call behavior deliberately. Add a regression in which no actual publication occurs inside the calibration window, so the sample remains pending and then becomes missing, plus a control where a genuine in-window publication supplies it. Evidence: `work/task4-review/check_calibration.py:17`, `work/task4-review/calibration-result.json:1`.

#### Minor (Nice to Have)

- ✅ None identified beyond the required fix.

### Checks and Scope

- ✅ Reviewed the supplied `6abce8b02d7c0d15508c3a385344875a76a924d5..0a73b76` package in bounded passes. The initial long tool output was truncated; its missing documentation region was recovered. The duplicated tracked implementation report was already read from the stated report path. Complete obsolete-file deletions were structurally checked rather than treating removed programs as executable behavior. No Git commands, source/index/HEAD writes, subagents, account actions or network probes were performed.
- ✅ Retained test evidence is complete and clean: `work/task4-full-tests.txt:7` reports **381 passed in 24.89s**; `work/task4-lint.txt:1`, `work/task4-format.txt:1` and `work/task4-types.txt:1` report successful lint, formatting and typing. The public-observe and final-doctor stderr files are empty. The successful suite was not rerun.
- ✅ Ran exactly one new focused synthetic check for the unanswered calibration concern: `.venv/bin/python work/task4-review/check_calibration.py`, exit 0. Its assertions reproduce I1 using real Ledger/Engine/Broker and existing external transport fixtures; no wall-clock sleeps or funded/network operations are used. Output is retained in `work/task4-review/calibration-result.json:1`.
- ✅ Additional source inspection stayed tied to named risks: the Engine.step hunk cuts off before its observation call, so `src/btc5m/engine.py:105-157` was read to complete that function; calibration timestamp acceptance was traced in `src/btc5m/ledger.py:906-998`; owner/readonly/path contracts in `src/btc5m/ledger.py:53` and `src/btc5m/ledger.py:128`; missing configuration during account-only reconciliation in `src/btc5m/ledger.py:344` and `src/btc5m/ledger.py:596` (no defect found); and shutdown/close behavior in `src/btc5m/market_data.py:218`, `src/btc5m/market_data.py:235`, `src/btc5m/market_data.py:250`, `src/btc5m/broker.py:68`, `src/btc5m/broker.py:139` and `src/btc5m/rpc.py:120`. Existing synthetic fixture helpers were read only to construct the new calibration check.
- ⚠️ Shutdown's controller deadline and durable cancellation have retained tests; a hard end-to-end upper bound for pathological transport teardown is not established by those tests or this diff. No additional teardown failure was reproduced or classified as a finding. Root's broader integration review should retain this limit when assessing the documented maximum shutdown time.

### Assessment

**Task quality: Needs fixes.**

**Reasoning:** The task has substantial behavioral coverage and clear operational/account boundaries, but the new cached-input execution path corrupts a specifically required observational measurement. Fix I1 and retain the root-owned final validation stages before approving the task.

### Fix Round 1 Re-review — 2e830258c0a759a7172d71ee164f85bb6d584776

- ✅ **Spec compliance: Approved for the scoped I1 correction.** `src/btc5m/engine.py:127` retains paired observation recording for direct callers; provider consumption instead calls pure current-time evaluation at `src/btc5m/engine.py:134`. The CLI's existing publication remains the sole observation writer in provider mode, so execution cannot move an earlier publication into the calibration window or duplicate its raw decisions. This closes I1 and supersedes the original task's Needs fixes verdict for that finding.
- ✅ **Task quality: Approved.** The fix adds one explicit branch, reuses the existing strategy evaluator, and preserves current-time safety checks, direct-call behavior, and post-preparation revalidation. No additional issue was found in the narrow Engine/test diff.
- ✅ **Regression evidence:** `tests/test_engine.py:921` covers both no in-window publication and a genuine in-window publication using actual LatestInput/Engine/Broker/SDK/Ledger with synthetic external transport. The first remains pending and then becomes missing, retaining exactly two original decisions; the control records the genuine timestamp, retains four decisions for two publications, and still submits one confirmed-candidate BUY. The added direct-call test preserves its existing observation boundary and proves stale inputs still cancel pending execution.
- ✅ **Checks reviewed:** `.superpowers/sdd/standalone/task-4-fix1.diff`, the appended fix report, and retained `work/task4-fix1-covering.txt:3` (**124 passed in 14.54s**), `work/task4-fix1-lint.txt:1`, `work/task4-fix1-format.txt:1`, and `work/task4-fix1-types.txt:1` are complete and clean. No tests were rerun; the added behavioral cases answer the named concern. No source/index/HEAD changes, broader source inspection, subagents, or account/network actions occurred.
- ⚠️ **Unchanged scope limits:** the final public 35-minute capture, fresh whole-branch review, memory/setup handoff, funded validation limits, and the earlier unproven pathological transport-teardown bound remain root-owned. This scoped approval makes no additional claim about those stages.
