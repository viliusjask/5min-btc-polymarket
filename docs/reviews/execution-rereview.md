# Task 3 scoped rereview, fix round 1

## Finding verdicts

- **P1: Do not finalize an entire transaction from one CONFIRMED trade — ADDRESSED.** `src/btc5m/broker.py:543` retains previously pending IDs. `:575-611` reads and deduplicates the full account result before grouping by transaction; any matching provisional status defers its whole transaction, and unmapped pending IDs prevent unsupported aggregate attribution. `:614-624` validates the complete deduplicated receipt set before publishing it, and receipt quantity cannot exceed the available matching CONFIRMED account quantity. No fee is invented by prorating an aggregate receipt. `tests/test_broker.py:654` covers BUY and SELL with MINED/MATCHED/RETRYING companions across account pages, asserts unchanged cash/fees/PnL/inventory while provisional, then exact once-only accounting when both confirm. `:723`, `:793` and `:833` cover insufficient confirmed quantity, separate-transaction settlement with a subsequently missing pending ID, and conflicting same-index receipt logs. The original reproduced defect no longer exists in this path.

- **P1: Preserve the current non-tick metadata contract during final preparation — ADDRESSED.** `src/btc5m/broker.py:312-313` requires exactly the supported `{r,e,to}` fee schema with `to is True`. `:322` checks `/markets.minimum_order_size` against the approved Market in addition to the existing book and `mos` checks. No SELL tick acceptance code was relaxed. `tests/test_broker.py:745` begins with an actual-core eligible snapshot, introduces each reviewed minimum/fee change before preparation, and verifies rejection before POST for both BUY and SELL; its SELL cases also exercise the previously authorized finer current SDK tick. The base fixture now supplies the required real minimum field.

- **P1: Read one consistent SQLite snapshot for the public summary — ADDRESSED.** `src/btc5m/ledger.py:294-304` encloses all summary reads in a savepoint. This establishes a transaction for a standalone readonly call and nests within a caller-owned transaction without committing it. `tests/test_ledger.py:563` commits an actual BUY settlement between the reader's position and unresolved-order queries and requires a coherent before-state summary, followed by the correct after-state on the next snapshot; both standalone and existing read transactions are covered. `:602` separately proves that calling summary does not commit the caller's pending write. The readonly path still acquires no trading-owner lock.

- **Root's persistent full-position close ruling — ADDRESSED.** `src/btc5m/engine.py:271-272` uses a stored STOP/PROFIT/TIME/SHUTDOWN request before recalculating price triggers. `src/btc5m/ledger.py:718` preserves the initial reason while updating the latest obstacle; later requested reasons remain in the existing event record. `src/btc5m/engine.py:224-227` records a SELL preparation failure on that position. Initial STOP/PROFIT still passes the existing whole-held-quantity quote checks; only subsequent attempts may use available protected partial depth. `tests/test_engine.py:665` covers both triggers after a real synthetic partial SELL, with and without restart, followed by a non-triggering .80 quote and only one available share. It requires a one-share retry at the current .79 floor and no duplicate uncertain POST. `:731` covers both triggers through metadata preparation failure and restart. `tests/test_ledger.py:614` proves later shutdown/resolution obstacles preserve the original close reason.

## New breakage in the fix diff

**None found.** The receipt change stages a whole transaction before publication, the summary savepoint preserves outer transaction ownership, and the close-policy change retains the existing quantity, metadata, freshness and unresolved-order controls. No new Critical, Important or Minor finding was identified in the fix diff.

## Out-of-scope observations

**No new out-of-scope defect identified.** Existing Task 4 integration obligations remain assigned: independent observations and held-book scheduling, fresh input selection after slow account work, delivery of intervening confirmation invalidations, persisted metadata diagnostics, readonly/owner-lock command routing, one-time startup stop clearing, safe reports and bounded shutdown. The root's fresh-input-provider clarification is separate work and has no source implementation in this diff; it was not treated as a reopened finding in this round. Funded venue behavior remains unverified.

## Checks and scope

- Reviewed the verbatim findings and close-policy ruling in `.superpowers/sdd/standalone/task-3-fix-1.md`, the appended fix report at `.superpowers/sdd/standalone/task-3-report.md:197`, and the supplied fix package for base `1770a1414c06b38bf154bd1e76f55cb809b2aa56` through head `fbe3e9eed89b34405376907bd943c87aca4677c8`. Inspected source/test changes in bounded diff passes; no fresh review of untouched source or upstream repository was performed. Root-authored documentation was considered for consistency with the fix/ruling, not as a new upstream audit.
- Verified the covering test command and behavior against the added regression code. Read `work/task3/fix1-covering-tests.txt` directly: **144 passed in 17.13s**, with no warnings. The appended report records the prior failing reproductions and clean Ruff/format/mypy checks after the fixes.
- No new code doubt justified another execution. Ran no test suite, focused reproduction, static checker, git command, account/network call or subagent. No source/index/HEAD/branch changes occurred. The sole write is this ignored rereview report.

## Verdict

**Fix round: All findings addressed, no new Critical/Important breakage.** All three original Important findings and the clarified persistent-close policy are satisfied by this scoped diff. **Task 3 source gate: Approved**, subject to the already assigned Task 4 integration verification and later whole-branch review; this is not funded trading approval.
