# Final scoped re-review

**Spec compliance: Approved. Code quality: Approved.** All three findings from the integrated review are addressed. No new Critical, Important or Minor defect was established in this fix package. The original whole-branch Needs fixes verdict is superseded for I1, I2 and I3 by this scoped approval; funded trading remains unverified.

Reviewed exact fix range `10817b7227b82fd255581510048d3b4354c8c3a3..3f49231b4a57cf6e00934dd86316317e3e275d19` in `/home/vilius/projects/5min-btc-polymarket/.worktrees/standalone`, using `.superpowers/sdd/standalone/final-fix.diff`. Read the final-rereview brief, implementation report, original findings, included capture/ruling documentation and retained verification. The seven implementation-owned paths are `src/btc5m/cli.py`, `broker.py`, `engine.py`, `market_data.py`, `tests/test_cli.py`, `README.md` and `SKILL.md`; root documentation in the package was treated as context. This review did not reopen untouched strategies/accounting or repeat the broad review.

## Finding verdicts

### I1 / P1: Immediate current-round official conflict invalidation — ADDRESSED

`src/btc5m/cli.py:213-228` compares the authoritative observation's slug with the currently cached market and immediately invalidates the cache for either `anchor/status=conflict` or `metadata_unavailable/code=UNSUPPORTED_RULE`. This occurs synchronously in the existing observer before the producer resumes awaited work. It reuses the established generation mechanism, so a later valid publication cannot erase the episode and the Engine's existing consumption/post-preparation guards reject the obsolete authorization. A historical round's event does not invalidate an unrelated current candidate.

`tests/test_cli.py:732-829` supplies 16 combinations covering anchor/rule conflict, blocked account/preparation I/O, later overwrite/no overwrite, and current/historical round. These use the actual SDK event model, `MarketData._poll_due`, observer, cache, core, Engine, Broker and SQLite with synthetic external transport. Current conflicts advance generation to1 and produce zero POSTs; preparation becomes REJECTED with no unresolved reserve, or pending confirmation is cancelled during account I/O. Historical controls retain generation0 and make their one synthetic POST. This closes both the original reproduced anchor race and the inspected delayed-rule counterpart without changing entry policy, freshness or economic bounds.

### I2 / P2: Shared cooperative shutdown budget and ownership-safe drain — ADDRESSED under the approved policy

`src/btc5m/cli.py:558-585` establishes one absolute event-loop deadline; duration expiry does not gain an extra controller-poll interval. `src/btc5m/cli.py:407-418` spends the remaining budget, cancels unfinished tasks, and then requires cancellation acknowledgement rather than detaching them. `src/btc5m/cli.py:642-669` uses that same limit for runtime-task drain and subsequent concurrent resource closure, including normal flat completion. It records deadline expiration, retains exit2 on unfinished cleanup, emits the final summary and releases the Ledger owner only after the execution tasks have conclusively stopped.

Repeated cancellation of the outer caller cannot interrupt that ownership boundary: finalization runs in its own shielded task at `src/btc5m/cli.py:701-714`, and cancellation propagates after safe finalization. `src/btc5m/engine.py:304-312` records an interrupted submitted request as UNKNOWN before propagating cancellation. It preserves the existing durable-before-POST boundary, held quantity/reserve, and no-replay behavior. `src/btc5m/broker.py:139-147` and `src/btc5m/market_data.py:235-247` attempt independent subordinate closes concurrently and collect their outcomes, so one awaiting close cannot prevent all other resources from being attempted.

Behavioral evidence:

- `tests/test_cli.py:917-997` covers delayed public-client, secure-client, RPC and stream-handle close after becoming flat. It checks cancellation by the shared deadline, final report, exit2, continued ownership during cleanup, subsequent owner acquisition, stopped streams, closed independent HTTP resources and zero orders.
- `tests/test_cli.py:1000-1097` cancels a real synthetic submitted SELL, deliberately holds cancellation acknowledgement, and cancels the outer caller again. A second owner remains blocked during both intervals. After acknowledgement, the caller receives cancellation, the journal can be reopened, and the order remains UNKNOWN with five outstanding/held shares and exactly one POST.
- `tests/test_cli.py:384-469` adds cancellation during reconciliation of an existing UNKNOWN BUY. The persisted stop, UNKNOWN state, USD4.9969 reserve and original single POST survive and ownership is released after drain.

The revised operator contract at `README.md:164-179`, CLI help at `src/btc5m/cli.py:172`, and `SKILL.md:30-35` accurately describes the root-approved cooperative guarantee. A coroutine that suppresses cancellation, a blocked event loop, or stalled synchronous storage/output can still delay termination. Ownership remains held until execution stops; external termination, confirmation of process exit, inspection and explicit reconciliation are documented recovery steps. This limitation is explicitly authorized in the brief and is not presented as an achieved hard real-time guarantee. It does not leave the original unbounded cooperative transport-close defect unresolved.

### I3 / P2: Persistent explicit final-reference conflict in retrospective reports — ADDRESSED

`src/btc5m/cli.py:352-374` preserves an explicit conflict independently of whether the retained price changed, accepts a first-seen conflict, and never heals a conflicted round with later observations. Numeric differences use the producer's existing USD1e-8 tolerance, including finite-value checks, so alternate compatible numeric representations do not manufacture conflicts. The output retains safe producer/original evidence rather than reducing the conflicted row to an uninformative label.

`tests/test_cli.py:834-875` runs the actual metadata producer through SQLite, reopens the historical journal, and tests contradictory and compatible sequences with and without producer restart. `tests/test_cli.py:878-914` verifies first-seen explicit conflict and a later apparently official value from a restarted producer cannot erase it. The reducer works on existing journals without a schema migration or backfill. No fill, accounting or calibration-publication path changes as part of this correction.

## Introduced-breakage assessment

No new confirmed defect was identified. The fixes reuse the existing cache generation, conflict tolerance, order acknowledgement and Ledger interfaces. No new configuration, dependency, public schema, trading policy or authentication capability was introduced. The order cancellation branch remains conservative; it neither retries an ambiguous POST nor releases uncertain exposure. The cleanup policy prioritizes conclusively stopped execution before releasing the wallet lock, and its timing limits match the approved documentation.

The root's included35-minute capture report clearly identifies its earlier source, zero entries and lack of explicit final-reference conflicts. It does not claim that anonymous observation validates live execution or these corrections. This review does not add a new deferred source finding. Previously closed task findings remain closed; the model, indexing, manual-claim, retention and funded-validation limits from the full review remain applicable.

## Evidence and checks actually performed

- Read the complete supplied fix diff in bounded passes, including every implementation/test hunk and included operator/ruling/capture documentation. Mapped line references from that package rather than regenerating a diff. No additional unchanged-source inspection was needed.
- Read `work/final-fix-verification.json` and its actual suite/lint/format/types/lock outputs. All retained commands exited0: **412 tests passed in28.79s**, lint clean, **21 files already formatted**, mypy clean for **12 source files**, lock resolution **45 packages**. The metadata records one integrated execution sequence on the final tested source, with the exact commands and durations.
- Read the retained confirmed-red ending (**16 failed,10 passed,31 deselected**) and final CLI-green result (**59 passed in12.92s**). The behavioral changes and final tests match the intended previously failing scenarios; the initial fixture-field correction is disclosed in the implementation report and was not counted as evidence of a production bug.
- Read `work/final-observe-35m/report-reread-result.json`: the root's retrospective public report on `3f49231` is byte-identical to the earlier conflict-free report, SHA256 `5e30fe4f22d9b6da22eb3fabc7d1e4428a1098da92dedfb201f99cb8165102a3`. This is compatibility evidence for that journal, not a replacement for the synthetic conflict cases. Root separately reports completed sync/help/public-doctor checks and closed probe sessions; this re-review did not rerun them or claim funded readiness from them.
- No unresolved introduced risk required another focused execution. No existing suite, new reproduction, static checker, network/account call, credential read, Git command, source/index/HEAD change or subagent was used. The sole write in this re-review is this requested ignored report.

**Remaining limits:** cancellation cooperation and final synchronous work are required for bounded termination; interrupted cleanup can leave resources for OS reclamation. Live credentials/account relationships, funded acceptance/fills/settlement, claims and strategy profitability remain outside development verification. The root owns final export, memory/CHEATSHEET and checklist completion. No further source fix is requested by this scoped review.
